import { RequestlineSearchPipe } from './requestline-search.pipe';

describe('RequestlineSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new RequestlineSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
